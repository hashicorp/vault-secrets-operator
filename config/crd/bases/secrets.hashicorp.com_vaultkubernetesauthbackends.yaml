# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: vaultkubernetesauthbackends.secrets.hashicorp.com
spec:
  group: secrets.hashicorp.com
  names:
    kind: VaultKubernetesAuthBackend
    listKind: VaultKubernetesAuthBackendList
    plural: vaultkubernetesauthbackends
    singular: vaultkubernetesauthbackend
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultKubernetesAuthBackend is the Schema for the vaultkubernetesauthbackends
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VaultKubernetesAuthBackendSpec defines the desired state
              of VaultKubernetesAuthBackendSpec
            properties:
              disableIssValidation:
                default: false
                description: DisableISSValidation Disable JWT issuer validation. Allows
                  to skip ISS validation.
                type: boolean
              disableLocalCaJwt:
                default: false
                description: DisableLocalCAJWT Disable defaulting to the local CA
                  cert and service account JWT when running in a Kubernetes pod.
                type: boolean
              issuer:
                description: Issuer Optional JWT issuer. If no issuer is specified,
                  then this plugin will use kubernetes/serviceaccount as the default
                  issuer. See these instructions for looking up the issuer for a given
                  Kubernetes cluster.
                type: string
              kubernetesCaCert:
                description: 'kubernetesCACert PEM encoded CA cert for use by the
                  TLS client used to talk with the Kubernetes API. NOTE: Every line
                  must end with a newline: \n if omitted will default to the content
                  of the file "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
                  in the operator pod'
                type: string
              kubernetesHost:
                default: https://kubernetes.default.svc:443
                description: KubernetesHost Host must be a host string, a host:port
                  pair, or a URL to the base of the Kubernetes API server.
                type: string
              namespace:
                description: Namespace to auth to in Vault
                type: string
              path:
                description: Mount to use when authenticating to auth method.
                type: string
              pemKeys:
                description: PEMKeys Optional list of PEM-formatted public keys or
                  certificates used to verify the signatures of Kubernetes service
                  account JWTs. If a certificate is given, its public key will be
                  extracted. Not every installation of Kubernetes exposes these keys.
                items:
                  type: string
                type: array
              vaultAuthRef:
                description: VaultAuthRef of the VaultAuth resource If no value is
                  specified the Operator will default to the `default` VaultAuth,
                  configured in its own Kubernetes namespace.
                type: string
            required:
            - path
            type: object
          status:
            description: VaultKubernetesAuthBackendStatus defines the observed state
              of VaultKubernetesAuthBackendSpec
            properties:
              error:
                type: string
              path:
                type: string
              valid:
                description: Valid auth mechanism.
                type: boolean
            required:
            - error
            - path
            - valid
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
