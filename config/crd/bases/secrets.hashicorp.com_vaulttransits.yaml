# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: vaulttransits.secrets.hashicorp.com
spec:
  group: secrets.hashicorp.com
  names:
    kind: VaultTransit
    listKind: VaultTransitList
    plural: vaulttransits
    singular: vaulttransit
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultTransit is the Schema for the vaulttransits API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VaultTransitSpec defines the desired state of VaultTransit
            properties:
              key:
                description: 'Key for encrypt/decrypt operations via Vault''s Transit
                  secrets engine. TODO: rename to KeyName'
                type: string
              mount:
                description: Mount path of the secret's engine in Vault.
                type: string
              namespace:
                description: Namespace where the secrets engine is mounted in Vault.
                type: string
              vaultAuthRef:
                description: VaultAuthRef to the VaultAuth resource If no value is
                  specified the Operator will default to the `default` VaultAuth,
                  configured in its own Kubernetes namespace.
                type: string
            required:
            - key
            - mount
            type: object
          status:
            description: VaultTransitStatus defines the observed state of VaultTransit
            properties:
              error:
                type: string
              valid:
                type: boolean
            required:
            - error
            - valid
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
