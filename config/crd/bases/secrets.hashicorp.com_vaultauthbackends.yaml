# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: vaultauthbackends.secrets.hashicorp.com
spec:
  group: secrets.hashicorp.com
  names:
    kind: VaultAuthBackend
    listKind: VaultAuthBackendList
    plural: vaultauthbackends
    singular: vaultauthbackend
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultAuthBackend is the Schema for the vaultauthbackends API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VaultAuthBackendSpec defines the desired state of VaultAuthBackendSpec
            properties:
              description:
                description: Type must be a host string,
                type: string
              namespace:
                description: Namespace to auth to in Vault
                type: string
              path:
                description: Mount to use when authenticating to auth method.
                type: string
              type:
                description: Type must be a host string,
                enum:
                - kubernetes
                type: string
              vaultAuthRef:
                description: VaultAuthRef of the VaultAuth resource If no value is
                  specified the Operator will default to the `default` VaultAuth,
                  configured in its own Kubernetes namespace.
                type: string
            required:
            - path
            type: object
          status:
            description: VaultAuthBackendStatus defines the observed state of VaultAuthBackendSpec
            properties:
              error:
                type: string
              path:
                type: string
              valid:
                description: Valid auth mechanism.
                type: boolean
            required:
            - error
            - path
            - valid
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
